-- ==========================
-- SCRIPT DE INICIALIZACIÓN PARA POSTGRESQL
-- ==========================
-- IMPORTANTE: Este script se ejecuta al crear el contenedor de PostgreSQL
-- pero las tablas serán creadas por Hibernate cuando el oauth-server arranque.
-- Por lo tanto, este script solo prepara la base de datos.

-- La base de datos oauth_server_db ya está creada por POSTGRES_DB en docker-compose.yml
-- Este archivo está aquí como referencia.
